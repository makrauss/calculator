package body list is
-- declarations
type NODE;

procedure Add (ID : ID_TYPE; VALUE : DATA_TYPE) is         -- add data to list
  begin
end Add;

function Get (ID : ID_TYPE; VALUE : DATA_TYPE) is         -- return DATA_TYPE --pops data from list
  begin
end Get;

function Search (ID : ID_TYPE) return BOOLEAN is         -- searches list for specific id
  begin
end Search;


end list;
